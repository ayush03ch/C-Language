//WAP to calculate square root of a number
#include <math.h>
#include <stdio.h>
int main()
{ int a,c;
printf("Enter any number: ");
scanf("%d", &a);
c=sqrt(a);
  printf("Square root of given number is %d", c);

return 0;
}